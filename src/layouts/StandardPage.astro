---
import Layout from "./Layout.astro";

interface Props {
    title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
    <main class="mx-auto p-4 w-[90%] text-xl leading-relaxed">
        <slot />
    </main>
</Layout>
